import EditableInstituteHistory from './components/EditableInstituteHistory';

const page = () => {
  return (
    <section>
      <EditableInstituteHistory />
    </section>
  );
};

export default page;
